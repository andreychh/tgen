{{/*SPDX-FileCopyrightText: 2026 Andrey Chernykh*/}}
{{/*SPDX-License-Identifier: MIT*/}}

{{- /*
	stub writes the place of a declaration the target does not spell yet. It is
	scaffolding and not Python: what it buys is that the sequence the file walks
	is the whole page from the first run, so every kind that lands afterwards
	reads as a diff of real output rather than as the first output there is. The
	last kind to land takes this template out of the target with it.
*/}}
{{- define "stub"}}{{/*gotype: github.com/andreychh/tgen/targets/pythonv2.Stub*/ -}}
# TODO: {{.Ref}} ({{.Kind}})
{{- end}}
