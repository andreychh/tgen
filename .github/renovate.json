{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests"
  ],
  "minimumReleaseAge": "7 days",
  "rebaseWhen": "behind-base-branch",
  "semanticCommits": "enabled",
  "postUpdateOptions": [
    "gomodTidy"
  ],
  "packageRules": [
    {
      "description": "Batch every non-security update into a single weekly PR",
      "matchUpdateTypes": [
        "major",
        "minor",
        "patch",
        "pin",
        "digest"
      ],
      "groupName": "all dependencies",
      "schedule": [
        "before 6am on monday"
      ]
    },
    {
      "description": "Runtime deps of generated code — raising the lower bound drops user support, update manually",
      "matchDepTypes": [
        "dependencies"
      ],
      "matchFileNames": [
        "stands/python/pyproject.toml"
      ],
      "enabled": false
    },
    {
      "description": "Bump the root go.mod go directive so the language version stays in sync with the mise toolchain",
      "matchManagers": [
        "gomod"
      ],
      "matchFileNames": [
        "go.mod"
      ],
      "matchDepTypes": [
        "golang"
      ],
      "matchDepNames": [
        "go"
      ],
      "rangeStrategy": "bump"
    },
    {
      "description": "Go stand pins the minimum supported Go version — bump the language manually, not via Renovate",
      "matchManagers": [
        "mise",
        "gomod"
      ],
      "matchFileNames": [
        "stands/go/mise.toml",
        "stands/go/go.mod"
      ],
      "matchDepNames": [
        "go"
      ],
      "enabled": false
    },
    {
      "description": "Python stand pins the minimum supported Python version — bump the language manually, not via Renovate",
      "matchManagers": [
        "mise"
      ],
      "matchFileNames": [
        "stands/python/mise.toml"
      ],
      "matchDepNames": [
        "python"
      ],
      "enabled": false
    }
  ],
  "vulnerabilityAlerts": {
    "enabled": true
  },
  "osvVulnerabilityAlerts": true,
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^\\.github/workflows/.*\\.yml$/"
      ],
      "matchStrings": [
        "uses: jdx/mise-action@.*\\n\\s+with:\\n\\s+version: (?<currentValue>.*)"
      ],
      "depNameTemplate": "jdx/mise",
      "datasourceTemplate": "github-releases"
    }
  ]
}
